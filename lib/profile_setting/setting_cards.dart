import 'package:flutter/material.dart';

class SettingCard extends StatelessWidget {
  const SettingCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}



// ListTile(
//   leading: Icon(Icons.settings), // replace with your desired icon
//   title: Text('Setting Name'), // replace with your desired text
//   trailing: Icon(Icons.arrow_forward_ios), // replace with your desired forward button icon
//   onTap: () {
//   },
// );
